# Ce fichier est conservé à titre de référence uniquement.
# L'orchestration est centralisée dans docker-compose.yml à la racine du projet.
# Lancer depuis la racine : docker compose up -d normalizer
